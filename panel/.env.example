# Copy to .env. Backend base URL used by the panel (axios).
# For local dev: http://localhost:8000  (or the port your backend runs on)
# For production: your public backend URL, e.g. http://SERVER_IP:8008
VITE_API_BASE=http://localhost:8000
